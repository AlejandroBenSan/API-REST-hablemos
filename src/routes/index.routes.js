import {Router} from 'express'
//import {datosEnDB} from '../controllers/index.controller.js'

const router = Router();

//OBTENEMOS LOS DATOS
//router.get('/estudiantes',datosEnDB)

export default router;